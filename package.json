{
  "name": "anthonyta",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "NODE_OPTIONS='--dns-result-order=ipv4first --no-network-family-autoselection' next dev",
    "build": "NODE_OPTIONS='--dns-result-order=ipv4first --no-network-family-autoselection' next build",
    "start": "next start",
    "lint": "eslint",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "postinstall": "node scripts/pdfjs-assets.mjs",
    "test:e2e": "playwright test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "vault-sync": "node --dns-result-order=ipv4first --no-network-family-autoselection --env-file=.env.local --import tsx scripts/vault-sync.ts",
    "hub-backup": "node --dns-result-order=ipv4first --no-network-family-autoselection --env-file=.env.local --import tsx scripts/hub-backup.ts",
    "aperture-sync": "node --dns-result-order=ipv4first --no-network-family-autoselection --env-file=.env.local --import tsx scripts/aperture-sync.ts"
  },
  "dependencies": {
    "@simplewebauthn/browser": "13.3.0",
    "@simplewebauthn/server": "13.3.2",
    "aws4fetch": "^1.0.20",
    "hash-wasm": "4.12.0",
    "next": "^16.3.1",
    "next-auth": "^5.0.0-beta.32",
    "pdfjs-dist": "^6.2.108",
    "postgres": "^3.4.9",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1",
    "web-push": "3.6.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@tailwindcss/postcss": "^4.3.3",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@types/web-push": "3.6.4",
    "eslint": "^9",
    "eslint-config-next": "^16.3.1",
    "prettier": "^3.8.4",
    "tailwindcss": "^4",
    "tsx": "^4.23.0",
    "typescript": "^5",
    "vitest": "^4.1.9"
  },
  "overrides": {
    "next-auth": {
      "@simplewebauthn/browser": "$@simplewebauthn/browser",
      "@simplewebauthn/server": "$@simplewebauthn/server"
    },
    "@auth/core": {
      "@simplewebauthn/browser": "$@simplewebauthn/browser",
      "@simplewebauthn/server": "$@simplewebauthn/server"
    }
  }
}
